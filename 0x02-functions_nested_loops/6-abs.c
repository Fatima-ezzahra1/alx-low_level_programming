#include "main.h"
/**
 * _abs-computes the absolute value of an integer
 *@n: the integer to compute the absolute value of n
 *Return: the absolute value of n
 */
int _abs(int n)
{
if (n < 0)
{
n = -n;
}
return (n);
}
